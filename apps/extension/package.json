{
  "name": "@app/extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "MV3 extension. Load dist/ unpacked after `npm run build -w @app/extension`.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@app/shared": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.28.2"
  }
}
